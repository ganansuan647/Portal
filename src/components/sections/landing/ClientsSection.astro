---
// Define props from Astro
const { title, subTitle } = Astro.props;

// Define TypeScript interface for props
interface Props {
  title: string;
  subTitle?: string;
}

import { Image } from 'astro:assets';
import TongjiImage from '@/images/Institute/Tongji.png';
import CivilEngImage from '@/images/Institute/CivilEngCollege.png';
import SeismicGroupImage from '@/images/Institute/SeismicGroup_Red.png';
---

<section
  class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
>
  <!-- Title and description -->
  <div class="mx-auto mb-6 w-full space-y-1 text-center sm:w-1/2 lg:w-1/3">
    <h2
      class="text-balance text-2xl font-bold leading-tight text-neutral-800 dark:text-neutral-200 sm:text-3xl"
    >
      {title}
    </h2>
    {
      subTitle && (
        <p class="text-pretty leading-tight text-neutral-600 dark:text-neutral-400">
          {subTitle}
        </p>
      )
    }
  </div>
  <div
    class="flex flex-col items-center justify-center gap-y-2 sm:flex-row sm:gap-x-12 sm:gap-y-0 lg:gap-x-24"
  >
    <!-- 客户群图片 -->
    <!-- 第一张 -->
    <Image src={TongjiImage} alt="客户1" class="w-80 h-24 object-cover" />
    
    <!-- 第二张 -->
    <Image src={CivilEngImage} alt="客户2" class="w-80 h-24 object-cover" />
    
    <!-- 第三张 -->
    <Image src={SeismicGroupImage} alt="客户3" class="w-80 h-24 object-cover" />
    
  </div>
</section>
